pre-commit:
  commands:
    cargo-fmt:
      glob: "*.rs"
      run: cargo fmt
      stage_fixed: true

pre-push:
  commands:
    cargo-clippy:
      glob: "*.rs"
      run: cargo clippy -- -D warnings
